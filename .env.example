# Postgres (Neon) — used for trainer stats
DATABASE_URL=

# Neon Auth — Auth URL from Neon Console: Project → Branch → Auth → Configuration
# Enables sign-in and cloud sync of stats (see db/schema.sql for the table).
NEON_AUTH_BASE_URL=
